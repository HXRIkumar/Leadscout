"""A3 — Account Research Engine.

One company in -> structured, evidence-verified fact base out. Script/API-first;
browser only as fallback (B7, reserved). Every factual claim carries a verbatim
quote + source URL, programmatically verified, failures dropped (SPEC §4.1).
"""
