"""C3 — Cross-engagement pattern discovery. RESERVED. Trigger: After 10 clients.

The moat's engine room: mines briefs + call notes + outcomes into reusable
pain/messaging patterns, enriching data/pattern_library/. Stable pattern IDs are
already emitted by A4 so this composes without rework (SPEC §4.4).
"""

from __future__ import annotations


def discover_patterns() -> list[dict]:  # pragma: no cover - reserved
    raise NotImplementedError("C3 reserved. Trigger: After 10 clients. Substrate: briefs + interactions + outcomes.")
