# Proposal templates (RESERVED — B2, After First Client)

Scoped, priced proposal shells built from the diagnosis (A4), cost estimate (A5),
and value benchmarks (SPEC §14). Trigger: the first real discovery call. Feeds C2
(price-to-win). The `proposals` table already exists (empty) per §4.4.
