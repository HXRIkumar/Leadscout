# Demo template — `doc_extraction` (RESERVED)

Second template family (SPEC A6: V1 project archetypes seed the first three). An
IDP pipeline demo for document-heavy workflows. **Not the wedge** — this is a
post-pilot upsell archetype (see `kernel/disqualifiers.md` reasoning). Built only
if/when a client's engagement calls for it.
