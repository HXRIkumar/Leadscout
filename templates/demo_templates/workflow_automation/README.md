# Demo template — `workflow_automation` (RESERVED)

Third template family (SPEC A6). An ops/workflow automation agent demo. **Not the
wedge** — invisible from public data, so it can't anchor cold outreach; it's an
upsell after the first project lands. Built only when an engagement calls for it.
